---
resources:
- name: pal-tracker-dotnet
  type: git
  source:
    uri: {{github-repository}}
    branch: master
    private_key: {{github-private-key}}

- name: deploy
  type: cf
  source:
    api: {{cf-api-url}}
    username: {{cf-username}}
    password: {{cf-password}}
    organization: {{cf-org}}
    space: sandbox

jobs:
- name: build-and-deploy
  plan:
  - get: pal-tracker-dotnet
    trigger: true
  - task: build and test
    file: pal-tracker-dotnet/ci/build.yml
  - put: deploy
    params:
      manifest: build-output/manifest.yml
      path: build-output
      environment_variables:
        WELCOME_MESSAGE: Hello from Concourse